{
  "name": "rxdb-storage-worklet-monorepo",
  "private": true,
  "packageManager": "pnpm@11.22.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm --workspace-concurrency=1 -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "24.3.0",
    "typescript": "6.0.3",
    "vitest": "3.1.1"
  }
}
